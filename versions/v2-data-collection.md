# Version 2: 데이터 축적 및 채점 고도화

## Overview

| 항목 | 내용 |
|------|------|
| 목표 | 채점 데이터 축적 및 품질 개선 기반 마련 |
| 선행 조건 | v1 완료, 충분한 사용자 확보 |
| 핵심 가치 | 데이터 기반 채점 정확도 향상 |

---

## Background

v0/v1에서 축적된 사용자 데이터를 활용하여:
1. AI 채점 품질을 검증하고
2. 향후 모델 개선을 위한 기반을 마련합니다.

---

## Features

### 1. 사용자 피드백 수집

채점 결과 화면에 피드백 UI 추가:

```
┌─────────────────────────────────────┐
│           채점 결과                 │
├─────────────────────────────────────┤
│                                     │
│  [점수 및 피드백 표시...]           │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  이 피드백이 도움이 되었나요?       │
│                                     │
│  😞  😐  🙂  😊  😍                │
│  1   2   3   4   5                  │
│                                     │
│  [선택적] 의견을 남겨주세요:        │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
│  [        제출        ]             │
│                                     │
└─────────────────────────────────────┘
```

| 수집 항목 | 설명 |
|----------|------|
| 유용성 점수 | 1-5점 |
| 자유 의견 | 선택적 텍스트 |
| 제출 시점 | 결과 확인 직후 |

### 2. 실제 TOPIK 점수 연동

시험 응시 후 실제 점수를 입력받아 AI 채점과 비교:

```
┌─────────────────────────────────────┐
│      실제 점수 입력 (선택)          │
├─────────────────────────────────────┤
│                                     │
│  TOPIK 시험을 보셨나요?             │
│  실제 점수를 입력하면 AI 채점의     │
│  정확도 개선에 도움이 됩니다.       │
│                                     │
│  시험 회차: [제XX회 ▼]              │
│                                     │
│  54번 점수: [    ] / 50점           │
│                                     │
│  [        입력        ]             │
│                                     │
└─────────────────────────────────────┘
```

| 수집 항목 | 설명 |
|----------|------|
| 시험 회차 | 드롭다운 선택 |
| 실제 점수 | 0-50점 |
| 연결 답안 | 해당 회차 연습 답안들 |

### 3. 채점 일관성 분석

동일 답안 재채점 기능:

- 이전 답안 "다시 채점하기" 버튼
- 점수 변동 추적
- 일관성 문제 발견 시 프롬프트 개선

### 4. 관리자 대시보드

| 지표 | 설명 |
|------|------|
| 일별 채점 수 | 사용량 추이 |
| 평균 피드백 점수 | 사용자 만족도 |
| AI vs 실제 점수 상관관계 | 채점 정확도 |
| 점수 분포 | 사용자 수준 분포 |
| AI 감지 플래그율 | 부정행위 추이 |

---

## Data Schema

### 피드백 테이블

```sql
CREATE TABLE submission_feedback (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    submission_id UUID NOT NULL REFERENCES submissions(id),
    user_id UUID NOT NULL REFERENCES users(id),

    rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),

    UNIQUE(submission_id)
);

CREATE INDEX idx_submission_feedback_rating ON submission_feedback(rating);
```

### 실제 점수 테이블

```sql
CREATE TABLE actual_scores (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),

    exam_session VARCHAR(20) NOT NULL,  -- "제XX회"
    question_number VARCHAR(10) NOT NULL DEFAULT '54',
    actual_score INTEGER NOT NULL CHECK (actual_score >= 0 AND actual_score <= 50),

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 연습 답안과 실제 점수 연결
CREATE TABLE submission_actual_score_link (
    submission_id UUID REFERENCES submissions(id),
    actual_score_id UUID REFERENCES actual_scores(id),
    PRIMARY KEY (submission_id, actual_score_id)
);
```

### 분석용 뷰

```sql
CREATE VIEW scoring_accuracy_analysis AS
SELECT
    s.id as submission_id,
    sr.total_score as ai_score,
    acs.actual_score,
    (sr.total_score - acs.actual_score) as score_difference,
    sf.rating as user_feedback_rating
FROM submissions s
JOIN scoring_results sr ON s.id = sr.submission_id
JOIN submission_actual_score_link link ON s.id = link.submission_id
JOIN actual_scores acs ON link.actual_score_id = acs.id
LEFT JOIN submission_feedback sf ON s.id = sf.submission_id;
```

---

## Analytics & Insights

### 1. 채점 정확도 분석

```
AI 점수 vs 실제 점수 상관계수 계산
- 목표: r > 0.7
- 평균 절대 오차(MAE) < 5점
```

### 2. 점수대별 정확도

| 실제 점수대 | AI 평균 점수 | 오차 |
|------------|-------------|------|
| 40-50 (고득점) | ? | ? |
| 25-39 (중간) | ? | ? |
| 0-24 (저점수) | ? | ? |

### 3. 피드백 분석

```sql
SELECT
    rating,
    COUNT(*) as count,
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 2) as percentage
FROM submission_feedback
GROUP BY rating
ORDER BY rating;
```

### 4. 개선 포인트 도출

- 낮은 피드백 점수 답안 분석
- AI vs 실제 점수 차이 큰 케이스 분석
- 공통 패턴 발견 → 프롬프트 개선

---

## Implementation

### API Endpoints

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/v1/submissions/:id/feedback` | 피드백 제출 |
| GET | `/api/v1/submissions/:id/feedback` | 피드백 조회 |
| POST | `/api/v1/actual-scores` | 실제 점수 입력 |
| GET | `/api/v1/actual-scores` | 실제 점수 목록 |
| POST | `/api/v1/actual-scores/:id/link` | 답안 연결 |

### Admin Dashboard

| 페이지 | 기능 |
|--------|------|
| Overview | 주요 지표 요약 |
| Accuracy | AI vs 실제 점수 분석 |
| Feedback | 사용자 피드백 분석 |
| AI Detection | AI 감지 통계 |

---

## Data Collection Strategy

### 피드백 수집률 향상

| 방법 | 설명 |
|------|------|
| 팝업 타이밍 | 결과 확인 후 10초 대기 |
| 간단한 UI | 원클릭 평점 |
| 인센티브 | 피드백 시 추가 무료 채점권 |

### 실제 점수 입력 유도

| 방법 | 설명 |
|------|------|
| TOPIK 시험 후 리마인더 | Push 알림 |
| 점수 입력 캠페인 | 기간 한정 이벤트 |
| 보상 | 점수 입력 시 프리미엄 기능 제공 |

---

## Privacy & Ethics

### 데이터 사용 동의

```
앱 가입 시 동의 항목:
- 답안 및 채점 결과의 서비스 개선 목적 활용
- 익명화된 데이터의 연구 목적 활용
```

### 데이터 보호

| 항목 | 조치 |
|------|------|
| 개인정보 분리 | 분석 시 익명화 |
| 접근 제한 | 관리자만 원본 데이터 접근 |
| 보관 기간 | 탈퇴 시 삭제 옵션 |

---

## Success Metrics

| 지표 | 목표 |
|------|------|
| 피드백 수집률 | 30% 이상 |
| 실제 점수 입력률 | 10% 이상 |
| 평균 피드백 점수 | 4.0/5.0 이상 |
| AI-실제 상관계수 | 0.7 이상 |
| MAE (평균 절대 오차) | 5점 이하 |

---

## Future Use (v3 준비)

수집된 데이터 활용 계획:

1. **Fine-tuning 학습 데이터**
   - 고품질 답안-점수 쌍
   - 피드백 높은 케이스 우선

2. **Calibration 데이터**
   - AI 점수 → 실제 점수 보정 함수
   - 점수대별 보정 계수

3. **프롬프트 개선**
   - 오차 큰 케이스 분석
   - Few-shot 예시 업데이트

---

## Dependencies

- v1 완료
- 충분한 사용자 기반 (MAU 500+)
- 관리자 대시보드 구현

---

*v2 데이터 축적 후 v3 (모델 최적화) 진행*
